#!/usr/bin/env bash

ORIG_DIR="$(pwd)"
cd "$(dirname "$0")"
BIN_DIR="$(pwd)"

trap "cd '${ORIG_DIR}'" EXIT

#-------------------------------------------------------------------------------
# rke
#-------------------------------------------------------------------------------

# rke remove --ignore-docker-version
# docker rm -f cluster-state-deployer $(docker ps -a --filter "name=k8s_" -q)

#-------------------------------------------------------------------------------
# k3d
#-------------------------------------------------------------------------------

exec k3d cluster delete rconway

#-------------------------------------------------------------------------------
# minikube
#-------------------------------------------------------------------------------

# exec minikube delete
