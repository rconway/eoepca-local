cluster_name: rconway

nodes:
  - address: endvrpad
    user: rconway
    role:
      - controlplane
      - etcd
      - worker

ingress:
  provider: none
  # provider: nginx
  # network_mode: hostPort
  # http_port: 31080
  # https_port: 31443
